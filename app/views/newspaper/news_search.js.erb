$('.search-results-wrap').html(
        '<%= escape_javascript(render partial: "search", object: @posts) %>' +
        '<div class="col-md-4 col-md-offset-4">' +
        '<%= will_paginate @posts, remote: true %>' +
        '</div>'
);
